<h1>List of all tasks</h1>

<% @tasks.each do |task| %>
  <tr>
    <li><%= task.item %></li>
    <li><%= task.price %></li>
    <li><%= button_to 'Delete', task, method: :delete, data: { confirm: 'Are you sure?' } %></li>
  </tr>
  <hr>

<% end %>
